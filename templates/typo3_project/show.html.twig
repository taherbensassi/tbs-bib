{% extends 'base.html.twig' %}

{% block title %}Typo3Project{% endblock %}

{% block body %}
    <h1>Typo3Project</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ typo3_project.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ typo3_project.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ typo3_project.description }}</td>
            </tr>
            <tr>
                <th>Avatar</th>
                <td>{{ typo3_project.avatar }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('typo3_project_index') }}">back to list</a>

    <a href="{{ path('typo3_project_edit', {'id': typo3_project.id}) }}">edit</a>

    {{ include('typo3_project/_delete_form.html.twig') }}
{% endblock %}
