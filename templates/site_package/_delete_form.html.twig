<form method="post" action="{{ path('site_package_delete', {'id': site_package.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ site_package.id) }}">
    <button class="btn">Delete</button>
</form>
