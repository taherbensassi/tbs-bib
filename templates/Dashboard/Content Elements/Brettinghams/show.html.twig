{% extends 'base.html.twig' %}

{% block title %}TbsModule{% endblock %}

{% block body %}
    <h1>TbsModule</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tbs_module.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ tbs_module.title }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ tbs_module.image }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ tbs_module.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tbs_module_index') }}">back to list</a>

    <a href="{{ path('tbs_module_edit', {'id': tbs_module.id}) }}">edit</a>

    {{ include('tbs_module/_delete_form.html.twig') }}
{% endblock %}
