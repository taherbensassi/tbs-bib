<form method="post" action="{{ path('export_content_element_delete', {'id': export_content_element.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ export_content_element.id) }}">
    <button class="btn">Delete</button>
</form>
