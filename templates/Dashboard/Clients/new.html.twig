{% extends 'base.html.twig' %}

{% block title %}Neuer Kunde{% endblock %}

{% block content %}
    <div class="content-wrapper">
        {% include 'Partial/Breadcrumb/_breadcrumb.htnl.twig' with {'breadcrumb': 'Kunden'} %}
        <div class="content">
            <div class="container-fluid">
                <div class="card">
                    <div class="card-header  d-flex justify-content-between align-items-right">
                        <h3 class="card-title">Neuen Client anlegen</h3>
                        <a href="{{ path('client_index') }}" class="btn btn-outline-dark  ml-auto">
                            Abbrechen
                        </a>
                    </div>
                    {{ include('Dashboard/Clients/_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
