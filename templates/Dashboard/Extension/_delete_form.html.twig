<form method="post" action="{{ path('tbs_extension_delete', {'id': tbs_extension.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tbs_extension.id) }}">
    <button class="btn">Delete</button>
</form>
