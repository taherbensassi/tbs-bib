<form method="post" action="{{ path('typo3_project_delete', {'id': typo3_project.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ typo3_project.id) }}">
    <button class="btn">Delete</button>
</form>
